import 'package:flutter/material.dart';

class BlasterPage extends StatelessWidget {
  const BlasterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
